# tunl
command-line tool written in Rust. It reads a config file with your service dependencies and opens all the tunnels you need with a single command. You get one process, one config, and every port forwarded
